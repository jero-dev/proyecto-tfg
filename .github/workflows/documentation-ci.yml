name: Documentation - Continuous Integration

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Set up Repository
      uses: actions/checkout@v3

    - name: Change to documentation directory
      run: cd doc

    - name: Install LaTeX
      run: sudo make install

    - name: Build TeX project
      run: make build